@model ListCategoriesViewModel

@section title {
    <title>Categories</title>
}

<div class="row">
    <div class="col">
        <h1>Categories</h1>
        <table class="table table-sm table-striped">
            <thead class="thead-light">
                <tr>
                    <th>Name</th>
                    <th class="text-center">Edit</th>
                </tr>
            </thead>
            @foreach (var accountGroup in Model.Categories)
            {
                <thead class="thead-dark">
                    <tr>
                        <th colspan="2">@accountGroup.Key</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var category in accountGroup)
                    {
                        <tr>
                            <td>@category.Name</td>
                            <td class="text-center"><a asp-action="Update" asp-route-id="@category.ID">Edit</a></td>
                        </tr>
                    }
                </tbody>
            }
        </table>
        <p><a asp-action="Create">New</a></p>
    </div>
</div>
