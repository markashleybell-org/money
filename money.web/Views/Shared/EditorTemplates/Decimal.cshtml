@model decimal

<div class="input-group">
    <div class="input-group-addon">&pound;</div>
    @Html.TextBox("", Model.ToString("0.00"), new { @class = "form-control", type = "number", step = "0.01", autocomplete = "off" })
</div>